% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vgi_rate_limiter.R
\name{create_rate_limiter}
\alias{create_rate_limiter}
\title{Create a Rate Limiter}
\usage{
create_rate_limiter(
  calls_per_batch = 10,
  delay_seconds = 1,
  show_messages = TRUE
)
}
\arguments{
\item{calls_per_batch}{Number of calls before applying delay}

\item{delay_seconds}{Seconds to pause between batches}

\item{show_messages}{Whether to show rate limiting messages}
}
\value{
A rate limiter environment with increment() method
}
\description{
Creates a rate limiter object that tracks API calls and applies
delays when thresholds are reached.
}
\examples{
\dontrun{
# Create a rate limiter
limiter <- create_rate_limiter(calls_per_batch = 10, delay_seconds = 1)

# Use in a loop
for (i in 1:50) {
  # Make API call
  result <- some_api_function()
  
  # Apply rate limiting
  limiter$increment()
}
}
}
